# ====================================================================
# Example Secret manifest — DO NOT COMMIT REAL SECRETS.
# In production, deliver via ExternalSecrets / SealedSecrets / Vault.
# ====================================================================
apiVersion: v1
kind: Secret
metadata:
  name: warranty-api-secrets
  namespace: warranty-intel
type: Opaque
stringData:
  API_KEY: "replace-me-with-strong-secret"
  OPENAI_API_KEY: "sk-replace-me"
  PINECONE_API_KEY: "pcn-replace-me"
  LANGCHAIN_API_KEY: "ls-replace-me"
